# Pigeon — night closeout, 2026-07-22 ~02:00

Honest state for the morning. What's verified-running vs what still needs you.

## The night run missed its scope (be clear about this)

The `/night-run` I fired was supposed to build pigeon (fan, age, rotate). It did **not**. The named `night-run` workflow is board-wide by design, and the scoped args did not override it — so it audited the whole board and instead:
- built an **X Engine** `value-test-gate` (on branch `value-test-gate`, unmerged) — real work, but not what was asked;
- **refused** a FAVOUR resolver lane because the launcher mounted it into a `pigeon` worktree (wrong repo) — same fleet-provisioning defect as Jul 15.

**Pigeon was not damaged** — verified `c8271c7` clean, `coo` runs, no stray worktree. But the run produced **zero** pigeon slices. That failure is the orchestrator's, not the agents'.

So `fan` below was built **by hand afterward**, not by the night run.

## Verified-running (driven with real invocations, not "tests pass")

- **v0.1 gather / check / guided setup** — dogfooded on the real 46-repo fleet. `check` returns the DEAD/LOW/anon-role trio; `gather` flags 3 diverged account keys.
- **v0.2 `fan`** (`9c629db`) — `coo site` declares where a key lives; `coo fan` writes the canonical nest value to every declared `.env`, backs up what it replaced. Verified on real temp files: writes canonical to all sites, adds a missing var, handles an unreadable file, **idempotent** on re-run, and **fails safe** on both no-key and no-sites. Values never printed. gitleaks self-scan clean.

## Needs Oscar (interactive — an agent cannot test these)

- **The guided paste flow** (bare `coo`). Run it, paste two real keys, and see if the flow *feels* right. This is the product's whole first impression and it's the one thing no agent can drive.

## Not built (still on the roadmap)

- **v0.3 age backend** — the portability unlock (Linux/VPS + encrypted shared nest). Needs `brew install age` and a design call on the shared-nest format. Better with you awake, not fired blind.
- **v0.4 rotate**, **v0.5 watch**, **v0.6 MCP** — as in ROADMAP.md.

## Also waiting

- **Codex viability review** — the prompt was handed over; its verdict (build past v0.3, or keep personal) is the gate on doing v0.3+.
- **X Engine `value-test-gate`** — the night run's actual output. Unmerged branch, Oscar's call whether it's wanted (it targets registry #013, a real open item).

## One-line state

Pigeon is a clean, committed, MIT repo with three verified verbs (`gather`, `check`, `fan`) plus a guided setup that needs one human test. The night-run mechanism drifted; the slice got built by hand instead. Nothing is broken, nothing is overclaimed.
